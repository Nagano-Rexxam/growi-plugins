{
  "name": "growi-plugin-confirm-delete-text",
  "version": "0.1.0",
  "description": "Require typing Delete before page deletion buttons become active in GROWI delete dialogs, and gate editor mode behind the enable-editing checkbox.",
  "license": "MIT",
  "type": "module",
  "main": "dist/client-entry.js",
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [
    "growi",
    "growi-plugin",
    "script",
    "delete",
    "confirmation"
  ],
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "growiPlugin": {
    "schemaVersion": "4",
    "types": ["script"],
    "clientEntry": "dist/client-entry.js"
  }
}
