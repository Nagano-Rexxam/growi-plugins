const d = "Delete", s = 'button[data-testid="delete-page-button"]', f = ".modal-content", m = ".modal-body", E = "#deleteRecursively", p = "#deleteCompletely";
let n = null, c = !1;
function y() {
  c || typeof document > "u" || (c = !0, u(), n = new MutationObserver(u), document.body != null ? n.observe(document.body, { childList: !0, subtree: !0 }) : window.addEventListener(
    "DOMContentLoaded",
    () => {
      c && n != null && document.body != null && n.observe(document.body, { childList: !0, subtree: !0 });
    },
    { once: !0 }
  ));
}
function L() {
  c && (c = !1, n == null || n.disconnect(), n = null);
}
function u() {
  for (const e of document.querySelectorAll(f)) {
    if (!(e instanceof HTMLElement) || !T(e) || e.querySelector(".growi-confirm-delete") != null)
      continue;
    const o = e.querySelector(s), i = e.querySelector(m);
    !(o instanceof HTMLButtonElement) || !(i instanceof HTMLElement) || i.appendChild(b(o));
  }
}
function T(e) {
  return e.querySelector(E) instanceof HTMLInputElement && e.querySelector(p) instanceof HTMLInputElement;
}
function b(e) {
  const o = document.createElement("div");
  o.className = "growi-confirm-delete";
  const i = document.createElement("div");
  i.className = "growi-confirm-delete__title", i.textContent = 'Type "Delete" to enable the delete button.';
  const t = document.createElement("input");
  t.className = "growi-confirm-delete__input", t.type = "text", t.placeholder = "Delete", t.autocomplete = "off", t.spellcheck = !1, t.setAttribute("autocapitalize", "off"), t.setAttribute("autocorrect", "off"), t.setAttribute("aria-label", "Delete confirmation text");
  const a = document.createElement("div");
  a.className = "growi-confirm-delete__status";
  const r = () => {
    const l = g(t.value) === d;
    e.disabled = !l, a.textContent = l ? "The delete button is ready." : 'Type "Delete" to enable the button.', o.dataset.state = l ? "ready" : "waiting";
  };
  return t.addEventListener("input", r), t.addEventListener("keydown", (l) => {
    l.key === "Enter" && l.preventDefault();
  }), o.append(i, t, a), r(), o;
}
function g(e) {
  return e.replace(/\s+/g, " ").trim();
}
const v = "growi-plugin-confirm-delete-text";
function D() {
  window.pluginActivators ?? (window.pluginActivators = {}), window.pluginActivators[v] = {
    activate: y,
    deactivate: L
  };
}
console.log("plugin started!!");
D();
